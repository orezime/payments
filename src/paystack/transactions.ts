// import { ITransactions } from '../../typings'

export class Transactions {
    options: Object

    constructor(options = {}) {
        this.options = options
    }

    initializeTransaction() {}
}
